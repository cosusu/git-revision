## BASICS ##
git init		# initialise local git repository
git add <file>		# add file to index (staging area) !use '.' to add all files!
git rm --cached <file>	# removes file from staging area
git status		# check status of working tree
git commit -m 'comment'# commit changes/files in index

## USEFUL ##
git branch -a		# list all branches
git checkout mybranch	# switches to mybranch

## REMTOTE REPO ##
git push		# push local repo to remote
git pull		# pull latest from remote repo
git clone		# clone repo into new directory

## GENERAL STEPS ##
1. git init in the folder
2. Add files to staging area
3. Edit files
4. commit the edits with a comment

## EXTRA NOTES ##
create .gitignore	# adding files/dir to this file excludes them from git
